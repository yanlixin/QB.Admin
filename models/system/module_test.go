package models

import (
	"testing"
)

func TestGetAllModules(t *testing.T) {
	//	m := DefaultModuleList()
	//	mdules := m.GetAllModules()
	//	if len(mdules) != 5 {
	//	t.Errorf("expected %d menu, got %d", 5, len(mdules))
	//	}
}
